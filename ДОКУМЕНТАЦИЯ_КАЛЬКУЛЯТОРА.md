# 🏠 ПОЛНАЯ ДОКУМЕНТАЦИЯ КАЛЬКУЛЯТОРА КАРКАСНЫХ СТРОЕНИЙ

## 📋 ОБЩАЯ АРХИТЕКТУРА

### Основные файлы:
- `index.html` - главный интерфейс калькулятора
- `script.js` - вся бизнес-логика (3580 строк)
- `style.css` - современный адаптивный дизайн
- `admin.html` - админ-панель для управления пользователями
- `price.txt` - прайс-лист с базовыми ценами

### Система наценок:
- **Базовая наценка:** 10% на все цены (`const COMM = 0.10`)
- **Функции наценки:**
  - `grossInt(x)` - наценка 10% с округлением до целого
  - `gross50(x)` - наценка 10% с округлением вверх к 50₽
  - `labelGross(text)` - автоматическое обновление цен в интерфейсе

---

## 🏗️ ТИПЫ СТРОЕНИЙ И РАЗМЕРЫ

### 1. СТАНДАРТНАЯ ЛИНЕЙКА

#### Хозблоки (hoblok)
**Размеры:**
- Ширина: 2, 2.1, 2.3, 2.5, 3, 4 м
- Длина: 2, 2.5, 3, 4, 5, 6, 7, 8 м

**Базовые цены (БЕЗ наценки):**
```
2×2: 52,800₽    3×2: 58,300₽    4×2: 64,900₽    5×2: 73,700₽    6×2: 78,100₽
3×3: 68,200₽    4×3: 82,500₽    5×3: 93,500₽    6×3: 99,000₽
```

**Особенности:**
- Внутри без отделки и утепления
- 1 окно в базовой комплектации
- Односкатная крыша из профнастила

#### Бытовки (bytovka)
**Размеры:**
- Ширина: 2, 2.1, 2.3, 2.5, 3, 4 м
- Длина: 2, 2.5, 3, 4, 5, 6, 7, 8 м

**Базовые цены (БЕЗ наценки):**
```
2×2: 70,400₽    3×2: 79,200₽    4×2: 89,100₽    5×2: 99,000₽    6×2: 103,400₽
3×3: 95,700₽    4×3: 108,900₽   5×3: 136,400₽   6×3: 139,700₽
```

**Особенности:**
- Утепление 50мм стены и пол
- Внутри оргалит
- 1 окно в базовой комплектации

#### Каркасные дома (house)
**Размеры:**
- Ширина: 6, 7, 8, 9, 10 м
- Длина: 4, 5, 6, 7, 8, 9, 10 м

**Тарифы (БЕЗ наценки):**
- Ломаная крыша: 10,450₽/м²
- Ломаная крыша + вагонка B-C класса: 11,000₽/м² (в базовой цене)
- Ломаная крыша + вагонка A класса: 10,450₽/м² + доплата за A класс
- Двускатная крыша: 13,750₽/м²

### 2. ЭКОНОМ-ЛИНЕЙКА

#### Хозблоки эконом (hoblok_economy)
**Размеры (ТОЛЬКО из прайса поставщика):**
- Ширина: 2, 2.2, 2.5, 3, 4, 5, 6, 7 м
- Длина: 2, 2.2, 2.5, 2.7, 3, 4, 5, 6 м

**Базовые цены (С наценкой 10%):**
**Ограничение:** Только размеры из прайса поставщика, расчет по площади НЕ ДОПУСТИМ
```
2×2: 30,800₽    2×2.2: 33,000₽   2×2.5: 48,400₽
3×2: 40,700₽    3×2.2: 44,000₽   3×2.5: 56,100₽   3×3: 64,900₽
4×2: 46,200₽    4×2.2: 49,500₽   4×2.5: 64,900₽   4×3: 77,000₽   4×4: 116,600₽
5×2: 51,150₽    5×2.2: 54,450₽   5×2.5: 73,700₽   5×3: 97,900₽   5×4: 152,900₽
6×2: 54,450₽    6×2.2: 56,650₽   6×2.5: 78,100₽   6×2.7: 96,800₽   6×3: 100,100₽   6×4: 158,400₽
7×2: 78,100₽    7×2.2: 82,500₽   7×2.5: 93,500₽   7×3: 118,800₽
```

#### Бытовки эконом (bytovka_economy)
**Размеры (ТОЛЬКО из прайса поставщика):**
- Ширина: 2, 2.2, 2.5, 3, 4, 5, 6, 7 м
- Длина: 2, 2.2, 2.4, 2.5, 2.7, 3, 4, 5, 6 м

**Базовые цены (С наценкой 10%):**
**Ограничение:** Только размеры из прайса поставщика, расчет по площади НЕ ДОПУСТИМ
```
2×2: 37,400₽    2×2.2: 40,700₽   2×2.5: 53,900₽
3×2: 47,850₽    3×2.2: 51,150₽   3×2.5: 67,100₽   3×3: 72,600₽
4×2: 54,450₽    4×2.2: 57,750₽   4×2.5: 73,700₽   4×3: 81,400₽   4×4: 127,600₽
5×2: 57,200₽    5×2.2: 60,500₽   5×2.5: 80,300₽   5×3: 105,600₽   5×4: 159,500₽
6×2: 62,590₽    6×2.2: 65,450₽   6×2.5: 84,150₽   6×2.7: 104,500₽   6×3: 113,300₽   6×4: 166,100₽
7×2: 82,500₽    7×2.2: 88,000₽   7×2.4: 95,700₽   7×2.5: 99,000₽   7×3: 124,300₽
```

---

## 💰 СИСТЕМА ЦЕНООБРАЗОВАНИЯ

### БАЗОВЫЕ ЦЕНЫ

#### Стандартная линейка (С наценкой 10% в базе)
- Цены в `CONFIG[type].basePrice` УЖЕ содержат наценку 10%
- В коде: `"3x3":68200` означает 62,000₽ + 10% = 68,200₽
- Дополнительная наценка НЕ применяется к итоговой сумме

#### Эконом-линейка (С наценкой 10% в базе)
- Цены уже содержат наценку 10%
- В коде: `"2x2":30800` означает 28,000₽ + 10% = 30,800₽
- Дополнительная наценка НЕ применяется

### РАСЧЕТ БАЗОВОЙ СТОИМОСТИ

#### 1. Каркасные дома
```javascript
basePrice = Math.ceil(area * RATE[roof].base / 10) * 10;
```
- Округление до 10₽
- Учитывается площадь и тип крыши

#### 2. Эконом-линейка
```javascript
basePrice = CONFIG[type].basePrice[`${w}x${l}`] || 0;
```
- Прямой поиск по размеру
- Если не найдено - поиск с перепутанными сторонами
- Если не найдено - basePrice = 0 (расчет по площади НЕ ДОПУСТИМ для эконом-линейки)

#### 3. Стандартная линейка
```javascript
basePrice = CONFIG[type].basePrice[`${w}x${l}`] || 0;
```
- Прямой поиск по размеру
- Если не найдено - поиск с перепутанными сторонами
- Специальная логика для ширины 2.5м: берется цена для 3м и уменьшается на 5000₽
- Если не найдено - расчет по площади (6050₽/м² для хозблоков, 8800₽/м² для бытовок - цены с наценкой 10%)

---

## 🚚 СИСТЕМА ДОСТАВКИ

### ТАРИФЫ ДОСТАВКИ

#### Стандартная линейка
- **1 машина (≤18м²):** 80₽/км
- **2+ машин (>18м²):** 140₽/км
- **Минимум:** 5,000₽ (хозблок), 6,000₽ (бытовка)

#### Эконом-линейка
- **Тариф:** 130₽/км
- **Минимум:** 7,000₽
- **Сборка:** +7,000₽ (при выборе газели)

#### Каркасные дома
- **Логика по площади:** 18м² = 1 машина
- **Тарифы:**
  - ≤24м²: 1 машина, 180₽/км
  - ≤42м²: 2 машины, 200₽/км
  - ≤60м²: 3 машины, 300₽/км
  - ≤78м²: 4 машины, 400₽/км
  - ≤96м²: 5 машин, 500₽/км
  - ≤114м²: 6 машин, 600₽/км
  - ≤132м²: 7 машин, 700₽/км
  - >132м²: +1 машина за каждые 18м², +100₽/км

### РАСЧЕТ ДОСТАВКИ
```javascript
// Стандартная линейка
rate = (veh === 2) ? CONFIG[type].delivery.perKm2 : CONFIG[type].delivery.perKm1;
cost = rate * km;
if (cost < minDeliv) cost = minDeliv;
del = Math.ceil(cost / 50) * 50; // Округление к 50₽

// Эконом-линейка
cost = CONFIG[type].delivery.perKm * km;
if (cost < CONFIG[type].delivery.min) cost = CONFIG[type].delivery.min;
del = cost; // БЕЗ наценки для доставки
```

---

## 🔧 ДОПОЛНИТЕЛЬНЫЕ ОПЦИИ

### УТЕПЛЕНИЕ (₽/м², БЕЗ наценки)
```javascript
const INSUL = {
  roll100: 550,      // Минеральная вата 100мм
  min150: 2000,      // Минеральная вата 150мм
  rock100: 1000,     // Каменная вата 100мм
  basalt150: 4000    // Базальтовая 150мм
};
```

### КРОВЕЛЬНЫЕ МАТЕРИАЛЫ (₽/м², БЕЗ наценки)
```javascript
const ROOFMAT = {
  galv: 0,           // Профнастил оцинкованный (базовый)
  ondulin: 0,        // Ондулин (базовый)
  profColor: 750,    // Цветной профлист
  tile_lom: 800,     // Металлочерепица (односкатная)
  tile_gable: 1200   // Металлочерепица (двускатная)
};
```

### ВНУТРЕННЯЯ ОТДЕЛКА (₽/м², БЕЗ наценки)
```javascript
const INREP = {
  osb_vag: 120,      // ОСБ → вагонка B-C
  osb_imit: 350,     // ОСБ → имитация бруса
  vag_imitBC: 250,   // Вагонка → имитация B-C
  vag_imitA: 400,    // Вагонка → имитация A
  vag_block: 1000,   // Вагонка → блок-хаус
  osb_vagA: 500,     // ОСБ → вагонка A
  vag_vagA: 380,     // Вагонка B-C → вагонка A
  osb_block: 1120    // ОСБ → блок-хаус
};
```

### ВНЕШНЯЯ ОТДЕЛКА (₽/м², БЕЗ наценки)
```javascript
const OUTREP = {
  vag_ext: 500,      // Вагонка B-C
  imitBC_ext: 250,   // Имитация бруса B-C
  block_ext: 1000,   // Блок-хаус
  osb_extA: 500,     // Вагонка A
  vag_extA: 380      // Вагонка B-C → вагонка A
};
```

### МАТЕРИАЛЫ ПОЛА (₽/м², БЕЗ наценки)
```javascript
const FLOOR_MAT = {
  plain: 0,          // Обрезная доска 25×150мм (базовый)
  osb: 500,          // ОСБ влагостойкий
  board50x150: 1700, // Шпунт доска 50×150мм
  planed35x140: 1400 // Строганая 35×140мм
};
```

### ПЕРЕГОРОДКИ (₽/пог.м, БЕЗ наценки)
```javascript
const PART = {
  p1: 2500,          // Односторонняя
  p2: 3200,          // Двусторонняя
  p3: 4000           // Двусторонняя с утеплением 100мм
};
```

### ОКНА И ДВЕРИ

#### Стандартная линейка (₽, БЕЗ наценки)
```javascript
const WOOD_PRICES = {
  win: {
    "60×90": 2000,
    "80×80": 3000,
    "100×100": 3500
  },
  door: {
    std: 2000,       // Обычная
    hinge: 2000,     // Распашная
    hingeWarm: 3000, // Распашная утепленная
    filen: 6000      // Филенчатая
  }
};

const METAL_PRICES = {
  rf: 12000,         // Дверь РФ
  rfThermo: 30000,   // РФ с терморазрывом
  thermoLux: 45000   // Термо Люкс
};
```

#### Эконом-линейка (₽, С наценкой 10%)
```javascript
const ECONOMY_WOOD_PRICES = {
  win: {
    "55×80": 1100,   // 1000 + 10%
    "30×30": 550     // 500 + 10%
  },
  door: {
    wooden_set: 3300,    // 3000 + 10%
    wooden_swing: 5500,  // 5000 + 10%
    interior_pine: 13200 // 12000 + 10%
  }
};

const ECONOMY_METAL_PRICES = {
  metal_rf: 14300    // 13000 + 10%
};
```

### СВАЙНЫЙ ФУНДАМЕНТ

#### Стандартная линейка (₽/шт, БЕЗ наценки)
```javascript
const PILES_STANDARD = {
  "1.5×76": 3000, "2.0×76": 3600, "2.5×76": 3700, "3.0×76": 4000,
  "2.0×89": 3600, "2.5×89": 4000, "3.0×89": 4300,
  "2.0×108": 4300, "2.5×108": 4700, "3.0×108": 5000
};
```

#### Эконом-линейка (₽/шт, С наценкой 10%)
```javascript
const PILES_ECONOMY = {
  "1.5×76": 3300, "2.0×76": 4000, "2.5×76": 4100, "3.0×76": 4450,
  "2.0×89": 4000, "2.5×89": 4400, "3.0×89": 4750,
  "2.0×108": 4800, "2.5×108": 5200, "3.0×108": 5500
};
```

### КОЛИЧЕСТВО СВАЙ ПО РАЗМЕРАМ

#### Малые строения (хозблоки/бытовки)
```javascript
const PILE_COUNT_SMALL = {
  "2x2": 4, "2x2.5": 4, "2x3": 4,
  "2x4": 6, "2x5": 6, "2x6": 6,
  "3x2": 4, "3x2.5": 4, "3x3": 9,
  "3x4": 9, "3x5": 9, "3x6": 9,
  "2.5x2": 4, "2.5x2.5": 4, "2.5x3": 4,
  "2.5x4": 6, "2.5x5": 6, "2.5x6": 6
};
```

#### Каркасные дома
```javascript
const PILE_COUNT = {
  "6x4": 12, "6x5": 12, "6x6": 16, "6x7": 20,
  "6x8": 20, "6x9": 24, "6x10": 26, "8x8": 28,
  "9x8": 30
};
```

---

## 🧮 АЛГОРИТМ РАСЧЕТОВ

### ОСНОВНАЯ ФУНКЦИЯ `calculate()`

#### 1. Определение параметров
```javascript
const type = selType.value;
const w = parseFloat(inpWidth.value);
const l = parseFloat(inpLength.value);
const area = w * l;
```

#### 2. Расчет базовой цены
- **Дома:** `basePrice = Math.ceil(area * RATE[roof].base / 10) * 10`
- **Эконом:** `basePrice = CONFIG[type].basePrice[`${w}x${l}`]`
- **Стандарт:** `basePrice = CONFIG[type].basePrice[`${w}x${l}`]`

#### 3. Расчет доставки
```javascript
// Определение количества машин
const totalArea = warmArea + (verArea || 0);
const isLargeGabarit = (totalArea > 18);
const veh = (type === "house") ? 1 : (isLargeGabarit ? 2 : 1);

// Расчет стоимости
rate = (veh === 2) ? CONFIG[type].delivery.perKm2 : CONFIG[type].delivery.perKm1;
cost = rate * km;
if (cost < minDeliv) cost = minDeliv;
del = Math.ceil(cost / 50) * 50;
```

#### 4. Расчет дополнительных опций
```javascript
// Утепление
const diff = INSUL[selInsul.value] - baseInsulPrice;
if (diff > 0) addExtra(diff * warmArea, "Утепление");

// Кровля
if (selRoofMat.value !== "galv") {
  addExtra(ROOFMAT[selRoofMat.value] * warmArea, "Кровля");
}

// Внутренняя отделка
const intRepPrice = INREP[selInRep.value] * warmArea;
if (intRepPrice > 0) addExtra(intRepPrice, "Внутренняя отделка");
```

#### 5. Применение наценки
```javascript
// Для стандартной линейки
const finalSum = grossInt(sum); // +10%

// Для эконом-линейки
const finalSum = sum; // Наценка уже включена в базовые цены
```

### ФУНКЦИЯ `addExtra(price, label)`
```javascript
function addExtra(price, label) {
  const finalSum = skipMarkup ? price : grossInt(price);
  extras += finalSum;
  extraMap[label] = (extraMap[label] || 0) + finalSum;
}
```

---

## 🔄 ЛОГИКА ЗАМЕН И ОБНОВЛЕНИЙ

### АВТОМАТИЧЕСКОЕ ОБНОВЛЕНИЕ ЦЕН
```javascript
function updatePrices() {
  // Обновление цен в селектах с наценкой 10%
  Array.from(selInsul.options).forEach(opt => {
    const p = INSUL[opt.value];
    opt.textContent = opt.textContent.replace(/\(.*?₽\/м²\)/, `(${formatPrice(grossInt(p))} ₽/м²)`);
  });
}
```

### ОБРАБОТКА ИЗМЕНЕНИЙ ТИПА
```javascript
function handleTypeChange() {
  const type = selType.value;
  const cfg = CONFIG[type];
  
  // Обновление доступных размеров
  if (cfg.isEconomy) {
    // Для эконом-линейки берем только размеры из basePrice
    availableWidths = [...new Set(Object.keys(cfg.basePrice).map(key => key.split('x')[0]))];
  } else {
    // Для стандартной линейки используем все размеры
    availableWidths = cfg.widths;
  }
}
```

### РАСЧЕТ ВЕРАНДЫ
```javascript
// Для эконом-линейки - веранды из прайса
if (verandaFromPrice) {
  basePrice = Math.round(verandaFromPrice.price * 1.1 / 50) * 50;
}

// Для стандартной линейки - расчет по площади
const verArea = vw * vd;
const priceKey = verRoof === 'verRoof' ? 'verRoof' : 'verGable';
addExtra(VERANDA[priceKey] * verArea, "Веранда");
```

---

## ⚠️ ВАЖНЫЕ ОСОБЕННОСТИ

### НАЦЕНКИ
1. **Стандартная линейка:** Наценка 10% уже включена в базовые цены строений
2. **Эконом-линейка:** Наценка 10% уже включена в базовые цены строений
3. **Дополнительные опции:** Наценка 10% применяется при расчете (утепление, кровля, отделка и т.д.)
4. **Доставка:** НЕ имеет наценки, только округление к 50₽
5. **Сваи:** Стандартная линейка - без наценки, эконом - с наценкой 10%

### ОКРУГЛЕНИЯ
- **Дома:** Округление до 10₽
- **Доставка:** Округление вверх к 50₽
- **Эконом-линейка:** Округление к 50₽ для базовых цен

### СПЕЦИАЛЬНЫЕ ЛОГИКИ
1. **Ширина 2.5м:** Берется цена для 3м и уменьшается на 5000₽
2. **Двускатная крыша:** +1800₽/м² для хозблоков/бытовок
3. **Внутренняя веранда:** Вычитается из общей площади
4. **Количество машин:** Определяется по площади (18м² = 1 машина)

### ВАЛИДАЦИЯ
- Проверка существования элементов перед использованием
- Обработка ошибок геокодирования
- Проверка принудительного выхода пользователей

---

## 📊 ПРИМЕРЫ РАСЧЕТОВ

### Пример 1: Хозблок 3×4 (стандартная линейка)
```
Базовая цена: 82,500₽ (уже с наценкой 10%)
Доставка: 80₽/км × 50км = 4,000₽ (минимум 5,000₽)
Утепление: 550₽/м² × 12м² = 6,600₽ × 1.1 = 7,260₽
Итого: 82,500 + 5,000 + 7,260 = 94,760₽
```

### Пример 2: Бытовка эконом 4×3
```
Базовая цена: 81,400₽ (уже с наценкой 10%)
Доставка: 130₽/км × 30км = 3,900₽ (минимум 7,000₽)
Сборка: +7,000₽ (при выборе газели)
Итого: 81,400 + 7,000 + 7,000 = 95,400₽
```

### Пример 3: Каркасный дом 6×8 (двускатная крыша)
```
Площадь: 48м²
Базовая цена: 48 × 13,750 = 660,000₽
Округление: Math.ceil(660,000 / 10) × 10 = 660,000₽
Доставка: 3 машины × 300₽/км × 40км = 36,000₽
Итого: 660,000 + 36,000 = 696,000₽
```

---

## 🎯 ЗАКЛЮЧЕНИЕ

Калькулятор представляет собой сложную систему с множественными логиками ценообразования, где:

1. **Стандартная линейка** уже содержит наценку 10% в базовых ценах строений
2. **Эконом-линейка** уже содержит наценку 10% в базовых ценах строений
3. **Дополнительные опции** (утепление, кровля, отделка) получают наценку 10% при расчете
4. **Доставка** рассчитывается по разным тарифам в зависимости от типа и площади (без наценки)
5. **Система свай** автоматически подбирает количество по размерам строения
6. **Итоговая сумма** НЕ получает дополнительную наценку - она уже включена в базовые цены

Все расчеты происходят в реальном времени при изменении параметров, с автоматическим обновлением цен в интерфейсе и применением соответствующих наценок.
