# 🔍 КОДОВАЯ ПРОВЕРКА КАЛЬКУЛЯТОРА
**Дата:** 8 октября 2025

---

## ✅ ПРОВЕРЕНО В КОДЕ

### 1. **ЭКОНОМ-ЛИНЕЙКА: Наценка 10%**

#### ✅ Все `addExtra` для эконом-опций используют `skipMarkup = true`:
- Двускатная крыша (22,000₽ / 16,500₽)
- Утепление потолка 50мм
- Внутренняя отделка ОСП-9
- Внутренняя отделка вагонка 'С'
- Утепление 100мм
- Огнебиозащита пола
- Огнебиозащита каркаса
- Паро- и ветроизоляция
- Перегородки
- Окна/двери (все типы)
- Ступени/пандусы

**Итого:** 18 вызовов `addExtra(..., true)` для эконом-линейки ✅

---

### 2. **СТАНДАРТНАЯ ЛИНЕЙКА: Дома с ломаной крышей**

#### ✅ Логика базовых цен (строки 2139-2149):
```javascript
let ratePerSqm = RATE[roof].base; // 10,450₽/м² (ОСБ)

if (roof === 'lom' && selInRep && selInRep.value === 'osb_vag') {
  // Если выбрана замена ОСБ → вагонка B-C, базовая цена становится 11,000₽/м²
  ratePerSqm = 11000;
}

basePrice = Math.ceil(paidArea * ratePerSqm / 10) * 10;
```

**Результат:**
- ✅ ОСБ (по умолчанию): 10,450₽/м²
- ✅ Вагонка BC (`osb_vag`): 11,000₽/м²
- ✅ Вагонка A и другие: 10,450₽/м² + доплата

#### ✅ Логика отображения в КП (строки 2681-2699):
```javascript
if (type === "house" && roof === "lom") {
  if (selInRep.value === 'osb_vag') {
    // Вагонка B-C класса: базовая цена 11,000₽/м² (уже учтено выше)
    // НЕ добавляем доплату, только меняем finalInt для отображения в КП
    finalInt = "vagBC";
  } else {
    // Для вагонки A класса и других материалов добавляем доплату как обычно
    ...
  }
}
```

**Результат:**
- ✅ Вагонка BC: `finalInt = "vagBC"` → "Вагонка B натуральная"
- ✅ Вагонка A: доплата + `finalInt = "vagA"` → "ОСБ → Вагонка A"

---

### 3. **EVENT LISTENERS: Автообновление КП**

#### ✅ Добавлены обработчики для всех элементов:
- ✅ Чекбоксы (29 обработчиков `addEventListener("change", calculate)`)
- ✅ Селекты (утепление, кровля, отделка, пол, веранда, сваи)
- ✅ Радио-кнопки (крыша, доставка, внутренняя отделка эконом)
- ✅ Инпуты (высота, длина перегородки)
- ✅ Динамические элементы (перегородки, окна/двери)

**Проверено:** Все основные элементы имеют обработчики ✅

---

### 4. **ДОСТАВКА: Эконом-линейка**

#### ✅ Логика сборки для Газели (строки 2169-2172):
```javascript
if (deliveryType === 'kit' && !verandaFromPrice) {
  basePrice += Math.round(7000 * 1.1); // 7000 + 10% = 7700
}
```

**Результат:**
- ✅ Газель: +7,700₽ к базовой цене
- ✅ Условие `!verandaFromPrice`: не добавляем, если веранда из прайса

#### ✅ Минимальная доставка (строки 2091-2110):
```javascript
if (!hasRoute) {
  const minDelivBase = (deliveryType === 'kit') ? 7000 : 10000;
  cost = Math.round(minDelivBase * 1.1);
}
```

**Результат:**
- ✅ Газель: 7,000₽ + 10% = 7,700₽
- ✅ Манипулятор: 10,000₽ + 10% = 11,000₽

---

### 5. **ВЕРАНДЫ: Эконом-линейка**

#### ✅ Цены в `VERANDA_CONFIG` (строки 376-412):
- Все цены уже включают 10% наценку
- Нет дублирования комбинаций

#### ✅ Отображение в выпадашке (строка 3395):
```javascript
const priceWithMarkup = v.price; // УЖЕ с наценкой, не умножаем на 1.1
```

**Результат:** Нет двойной наценки ✅

---

### 6. **ПЕРЕГОРОДКИ: Эконом-линейка**

#### ✅ Цены в коде (строки 2283-2296):
```javascript
const partitionPrices = {
  'organlit_2': 6600,    // 6000 + 10%
  'organlit_2.5': 7700,  // 7000 + 10%
  'organlit_3': 8250,    // 7500 + 10%
  'osb_2': 7700,         // 7000 + 10%
  'osb_2.5': 8800,       // 8000 + 10%
  'osb_3': 9900,         // 9000 + 10%
  'vagonka_2': 13200,    // 12000 + 10%
  'vagonka_2.5': 15950,  // 14500 + 10%
  'vagonka_3': 16500,    // 15000 + 10%
  'hoblok_2': 5500,      // 5000 + 10%
  'hoblok_2.5': 7150,    // 6500 + 10%
  'hoblok_3': 8250       // 7500 + 10%
};
```

#### ✅ Использование (строка 2305):
```javascript
addExtra(total, `Перегородка ${materialText.toLowerCase()} ${cleanOptionText} (${qty} шт)`, true);
```

**Результат:** Цены с наценкой, `skipMarkup = true` ✅

---

### 7. **СТАНДАРТНАЯ ЛИНЕЙКА: Обновленные цены**

#### ✅ Сваи (строки 246-250):
```javascript
const PILES_STANDARD = {
  "1.5×76":3350, "2.0×76":3500, "2.5×76":3800, "3.0×76":4100,
  "2.0×89":3700, "2.5×89":4000, "3.0×89":4400,
  "2.0×108":4500, "2.5×108":4900, "3.0×108":5400
};
```

#### ✅ Спецусловие для 76/1500 (строки 2511-2526):
```javascript
if (svaiType === "1.5×76" && cnt >= 16) {
  // Спецусловие: от 16 шт цена 3350₽/шт
  addExtra(3350 * cnt, `Сваи ${svaiType} (${cnt} шт)`);
} else {
  addExtra(pricePerPile * cnt, `Сваи ${svaiType} (${cnt} шт)`);
}
```

**Результат:** Спецусловие работает ✅

#### ✅ Пандус (строка 235):
```javascript
const RAMP = 2000; // пандус
```

**Результат:** Обновлено с 15,000₽ на 2,000₽ ✅

#### ✅ Окна ПВХ (строки 306-323):
```javascript
const WINDOWS = {
  "50×50":5500, "60×90":7500, "60×120":10000, "60×180":12000,
  "100×100":9000, "100×120_1":11000, "100×120_2":14000,
  "120×120":13000, "100×140":14000, "100×150":15000,
  "120×150":16500, "140×150":17000, "150×150":17500,
  "150×100":15000, "150×190":25000, "180×190":26000
};
```

**Результат:** Все новые размеры и цены добавлены ✅

#### ✅ Дверь ПВХ (строка 324):
```javascript
const PVC_DOOR = 32000;
```

**Результат:** Обновлено ✅

---

### 8. **УВЕЛИЧЕНИЕ ВЫСОТЫ**

#### ✅ Расчет по общей площади (строки 2478-2486):
```javascript
const totalArea = w * l; // ОБЩАЯ площадь (включая веранду)
const cnt10cm = Math.floor(extraH / 10);
if (cnt10cm > 0) {
  const price10 = pricePer10cm(totalArea);
  addExtra(price10 * cnt10cm, `Увеличение высоты на ${extraH} см`);
}
```

**Результат:** Использует `totalArea` (w × l) ✅

---

## 📊 ИТОГОВАЯ СТАТИСТИКА КОДОВОЙ ПРОВЕРКИ

- ✅ Эконом-линейка: наценка 10% применяется 1 раз
- ✅ Стандартная линейка (дома): логика ОСБ/Вагонка BC/A корректна
- ✅ Event listeners: добавлены для всех элементов
- ✅ Доставка: логика Газель/Манипулятор корректна
- ✅ Веранды: нет двойной наценки
- ✅ Перегородки: нет двойной наценки
- ✅ Обновленные цены: все применены
- ✅ Увеличение высоты: по общей площади

---

## 🎯 ВЫВОДЫ

**Код проверен на уровне логики и структуры. Все основные исправления применены корректно.**

**Следующий шаг:** Функциональное тестирование в браузере для проверки:
1. Корректности расчетов
2. Отображения в КП
3. Автообновления при изменении опций
4. Отсутствия дублирования цен
